---


---

<style>
    header .right{
        
        background-image: url(./../images/yinetyang_trans1000x563.png);
        /* background-attachment: fixed; */
        background-position: center;
        background-repeat: no-repeat;
        background-size: 100%;
        background-color: #000000;
    }

</style>

<header class="header">
   <div class="left">
       <h1 class="title">
           <!-- De Feu et d'Eau -->
         <span class="t1">De Feu</span>
         <span class="t2">&</span>
         <span class="t3">d'Eau</span>
       </h1>
   </div>
   <div class="right"></div>
     
</header>

<!-- <script>
import type { number } from "astro:schema"

    const t1 = document.querySelector('.t1')
    const t2 = document.querySelector('.t2')
    const title1 = "De Feu"
    const title2 = "& d'Eau"

    const sleep = (ms:number) => {
  return new Promise(resolve => setTimeout(resolve, ms))
}

    function typeWriter (element:HTMLElement, text:string, i=0) {
        if (i === 0) {
            element.textContent = '';
        }
        element.textContent += text[i];

        if (i === text.length - 1) {
            return ;
        }
        setTimeout(()=> typeWriter(element, text, i+1),200)
    }
    const control = async () => {
        await typeWriter(t1,title1)
        await sleep(2000)
        await typeWriter(t2,title2)
    }

    control();
</script> -->
    